@model IEnumerable<QuanLyDiem.Models.ChuyenNganh>

@{
    ViewData["Title"] = "Index";
}

<h3>Danh sách chuyên ngành</h3>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="w3-center"><a class="btn w3-round-large w3-medium btn btn-outline-info" asp-action="Create">Tạo mới chuyên ngành</a></div>
                </div>    
                <div class="table-responsive m-t-40 ">
                    <table id="myTable" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th onclick="sortTable(0)" class="column_name" style="width:42%">
                                    Tên chuyên ngành
                                </th>
                                <th onclick="sortTable(1)" class="column_name" style="width:42%">
                                    Khoa
                                </th>
                                <th style="width:16%"></th>
                            </tr>
                        </thead>
                        <tbody>
                    @foreach (var item in Model) {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TenChuyenNganh)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Khoa.TenKhoa)
                                </td>
                            <td>
                                <a class="btn btn-outline-info w3-medium w3-round-large" asp-action="Edit" asp-route-id="@item.MaChuyenNganh">Sửa</a>
                                <a class="btn btn-outline-info w3-medium w3-round-large" asp-action="Delete"  asp-route-id="@item.MaChuyenNganh">Xóa</a>
                            </td>
                            </tr>
                    }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>